extends layout

block content

  h1 #{product.name}
  p #{product.description}
  p #[strong Preço:] R$ #{product.formattedPrice}
  p #[strong Categoria:] #{product.category.name}
  p #[strong Quantidade:] 
    if(product.quantity > 0 && product.quantity <= 10)
      span.text-warning #{product.quantity}
    else if(product.quantity > 10)
      span.text-success #{product.quantity}
    else
      span.text-danger #{product.quantity}
  div(style='margin-left:20px;margin-top:20px')

    h4 Fornecedores

    dl
      each supplier in product.supplier
        dt
          a(href=supplier.url) #{supplier.name}
        dd #[strong CNPJ:] #{supplier.cnpj}
        dd #[strong Localização:] #{supplier.location}

  hr
  p
    a(href=product.url+'/delete') Deletar Produto
  p
    a(href=product.url+'/update') Atualizar Produto
