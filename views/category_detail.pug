extends layout

block content

  h1 #{category.name}

  div(style='margin-left:20px;margin-top:20px')

    h4 Produtos

    dl
      each product in products
        dt
          a(href=product.url) #{product.name}
      else 
        p Essa categoria não possui produtos.
  hr
  p
    a(href=category.url+'/delete') Deletar Categoria
  p
    a(href=category.url+'/update') Atualizar Categoria
